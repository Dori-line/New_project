{% extends 'base.html.twig' %}

{% block title %}Photos{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('show_pic') }}
{% endblock %}

{% block body %}
    <main>
        <h1>Photo</h1>
        <div class="the-photo">
            <img src="/Images/dbPictures/{{ picture.image }}" alt="">
            <p>{{ picture.description }}</p>
        </div>
    </main>



    <a href="{{ path('pictures_index') }}">back to list</a>

    <a href="{{ path('pictures_edit', {'id': picture.id}) }}">edit</a>

    {{ include('pictures/_delete_form.html.twig') }}
{% endblock %}
